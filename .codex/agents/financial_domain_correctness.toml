name = "financial_domain_correctness"
description = "Read-only financial-domain reviewer for money, civil dates, transactions, goals, contributions, fixed bills, and payments."
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Audit financial correctness and database/application invariant alignment. Read the
root AGENTS.md, data model and portability specs, migrations that constrain the
financial tables, and `lib/transactions/`, `lib/goals/`, `lib/fixed-bills/`, their
Server Actions, repositories, loaders, pages, and relevant tests.

Trace create, edit, reassignment, deletion, monthly display, and aggregation flows.
Check numeric(12,2) bounds, cent arithmetic, parsing, civil dates in
America/Sao_Paulo, month transitions, future/retroactive dates, safe-integer totals,
goal pace, bill status, multiple payments, stale derived state, workspace-linked
goal/fixed-bill integrity, ON DELETE SET NULL behavior, and concurrency where a
precheck and mutation can diverge. Compare application validation with final
database enforcement and identify contract drift explicitly.

Do not report product preferences or the documented civil-month limitation as a new
bug unless its conditions changed. Do not run hosted tests, edit, spawn agents, or
open issues. Use the discovery contract in AGENTS.md and include safe conceptual
regression tests.
"""
